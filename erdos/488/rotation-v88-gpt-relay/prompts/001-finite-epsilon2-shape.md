We are working on Erdős Problem 488. Do not try to solve A4/full EP488 here.

Current local progress:

1. v86 pointwise extension theorem:
   If a reduced top-window core S is pointwise safe, then every admissible
   top-window extension S union {a} is pointwise safe. Thus the old
   extension E(C)/eta growth barrier is bypassed.

2. v87 minimal-core theorem:
   Every connected induced deletion-minimal high-defect core has exact
   defect epsilon = 2. So deletion-minimal cores with epsilon >= 3 are
   impossible.

Definitions:

Reduced top-window:
- q is fixed.
- C is finite, primitive, connected as an induced collision component.
- q/2 < a < q for every a in C.
- n in [5q/2, 3q).
- B_n(C,q) has vertex set C and edge {a,b} iff lcm(a,b) <= n and
  q does not divide lcm(a,b).
- tau(C) is the number of triple fibers t <= n with q not dividing t and
  exactly three vertices of C dividing t.
- epsilon(C) = beta(B_n(C,q)) - tau(C), where beta is total cyclomatic
  number.

Deletion-minimal high-defect core:
- C0 is connected induced.
- epsilon(C0) = 2.
- For every vertex v, epsilon(C0 \ {v}) <= 1.

Top-window normalization:
- Every collision edge has endpoint ratio among
  (2,3), (3,4), (3,5), (4,5).
- After 5-smooth normalization, vertices are exponent vectors in Z^3.
- Allowed edge difference vectors are
  +/-(-1,1,0), +/-(-2,1,0), +/-(0,-1,1), +/-(-2,0,1).
- The normalized four-ratio graph has no K4.
- A triple fiber is exactly a triangle of vertices {L/3, L/4, L/5}.

Remaining A2-Induced barrier:

Finite epsilon-2 shape lemma.

Task:
Prove or disprove the following.

Every normalized connected induced deletion-minimal epsilon=2 core in the
four-ratio graph has bounded normalized diameter/size and belongs to a finite
shape family generated by two non-triple independent cycles together with
shortest connecting paths, with triple triangles accounted for by tau.

Allowed outputs:

A. A rigorous finite shape theorem with an explicit bound.
B. A concrete counterexample normalized core, given as 5-smooth exponent
   vectors, with all edges, triple triangles, beta, tau, epsilon=2, and
   deletion-minimality verified.
C. A weaker theorem that strictly advances classification.
D. A precise missing lemma and exact failure point.

Required:
- Work in the normalized four-ratio graph; define every invariant exactly.
- Check consistency with theta13, Kimi obstruction, and v56 near-miss:
  all are epsilon=2 deletion-minimal or contain such cores.
- Avoid dead routes: broad triple-stripping, unconditional pseudoforest A2',
  v52 run-count equality, Hunter density bridge, undefined x_1/x_3.
- Do not claim EP488 is solved.

Focus on graph-theoretic classification of normalized epsilon=2 deletion-minimal
cores. If a finite classification is false, produce the smallest explicit
counterexample family or obstruction.
