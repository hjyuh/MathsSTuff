EP488 audit. v90 proves: for fixed q, 5q/2 <= n < 3q, any C in
(q/2,q), D_C(m)/m <= 2D_C(n)/n for all m>n, by pointwise extension
from empty set.

Does this fully solve original F_Q for primitive Q and all
m>n>=max(Q)? Check exact chain.

Is arbitrary n>=q rigorously reduced to n<3q, or only conditional on
unresolved BBDS lemma extremizer_implies_bad_block?

If not solved, output exact missing lemma and failure point.

Do not claim solved unless all reductions are rigorous.

