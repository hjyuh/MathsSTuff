# Send To: GPT 5.4 Pro Extended Thinking

Attach:
- `unified-truth-v53-april17.md`
- first-wave responses if available

## Role

You are the historical dead-approach filter. GPT 5.4 contributed heavily to prior EP work; use that continuity. Your job is to prevent us from rediscovering a killed route with new notation.

## Task

Audit the current proposed route(s), especially any first-wave proposal, against all known dead approaches in v53.

Focus on whether a proposal secretly uses:

- v52 broken run-count equality;
- density bridge `D(m)/m≤W_T`;
- forest assumption;
- BadBlock descent;
- full-graph Hunter bound;
- direct q-shift/slot transport;
- unproved template finiteness;
- an asymptotic statement where a uniform bound is needed;
- ordinary multiples where q-excluded multiples are required.

## Deliverables

1. For each proposed route, classify:
   - genuinely new;
   - old but repaired;
   - killed;
   - unclear.

2. For every killed/unclear route, explain the exact failing step and give a regression example if possible.

3. If a route survives, propose the tightest theorem statement to send to formalization.

## Output Format

```
ROUTE CLASSIFICATION TABLE:
```

```
KILLED ROUTES:
```

```
SURVIVING ROUTES:
```

```
MISSING HYPOTHESES:
```

```
FORMALIZATION CANDIDATES:
```

```
BEST NEXT PROMPT:
```

