# EP-488: Quotient-Core Structural Analysis
## Source: GPT-5.4 Pro Extended — April 5, 2026

## TASK 1: Q_j CHARACTERIZED (PROVED)

Q_j generated by quotients a_i/gcd(a_i, a_j) for i < j (earlier elements only).

Prime p ∈ P_j ⟺ max_{i<j} v_p(a_i) > v_p(a_j)

i.e., some earlier element has MORE p-power than a_j.

BOUNDS ON q_j:
- q_j | rad(lcm(a_1,...,a_{j-1}))
- q_j can be VASTLY larger than M
- Example: A = {primes ≤ 29}, M = 29, q_{10} = 223,092,870
- q_j ≤ M is FALSE, q_j ≤ a_j is FALSE

Principal layer: Q_1 = ∅, q_1 = 1, ρ_1 = 1 always.

## TASK 2: STRUCTURAL INEQUALITY FALSE (KILL #47)

Σ ρ_j(r_j - 3q_j - 2) > 0 is FALSE.

Counterexample 1: A = {2,3,5}, sum = -12.
Counterexample 2: A = {primes ≤ 29}, sum ≈ -1.15 × 10^8.

Fails because φ(q_j) is a WORST-CASE-OVER-ENTIRE-PERIOD bound, but
tail layers only explore y ∈ [r_j, 10r_j] which can be tiny (≤ 10).
Most primes in q_j are INACTIVE on that window.

## TASK 3: THE FIX — ACTIVE-PRIME WINDOWED BOUND (PROVED)

### Key identity:
For y ≤ 10r, primes p > 10r cannot divide any n ≤ y, so:
K_P(y) = K_{P^{≤10r}}(y)

### Windowed discrepancy decomposition:
D_P(y) = [K_{P≤10r}(y) - ρ_{≤}·y] + (ρ_{≤} - ρ)·y

where ρ_{≤} = φ(q_{≤10r})/q_{≤10r}, only counting active primes.

### REFINED BOUND:

  sup_{y ∈ [r, 10r]} |D_P(y)| ≤ φ(∏_{p∈P, p≤10r} p) + 10r · Σ_{p∈P, p>10r} 1/p

Term 1: periodic discrepancy from ACTIVE primes only (≤ 10r)
Term 2: density mismatch drift from INACTIVE primes (harmonic tail, small)

### Example verification:
A = {primes ≤ 29}, layer 10 (a=29, r=1):
- Naive bound: φ(q_{10}) ≈ 3.6 × 10^7
- Active-prime bound: φ(2·3·5·7) + drift = 48 + ~3 ≈ 51
- Improvement: factor of ~700,000

### Properties:
- Scale-invariant (r_j and P_j unchanged by A → tA)
- Exact (not an approximation)
- Structurally determined by the primitive set

## TASK 3b: PHASE MIXING BLUEPRINT (CONJECTURAL)

1. Fourier expand each layer's active discrepancy
2. Large-sieve / Hilbert-space orthogonality over [M, 10M]
3. Convert L² → L∞ via Bernstein/Sobolev for trig polynomials

This would give sup|Σ ε_j| ≪ √(Σ ε_j²) instead of Σ |ε_j|.

## STATUS
- Task 1: PROVED
- Task 2: KILLED (Kill #47: naive structural inequality)
- Task 3a: PROVED (active-prime windowed bound)
- Task 3b: CONJECTURAL (phase mixing program)

## KILL COUNT: 47
