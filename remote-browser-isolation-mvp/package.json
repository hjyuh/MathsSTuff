{
  "name": "remote-browser-isolation-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal remote browser isolation MVP that launches Kasm Chromium workspaces in Docker and streams them via your app.",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/session-manager.js && node --check src/auth-manager.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "http-proxy-middleware": "^3.0.5"
  }
}
